export function multiplicationTable (size: number): number[][] {
    const level1: number[][] = [];
    const array = [];
    for (let i = 1; i < size; i++){
        array.push(i);
        for (let j = size; i > 0; i++){
            
        }
    }
    level1.push(array);
    console.log(level1);
    return level1;
}
  multiplicationTable(4);
